name: Test

on: [push]

jobs:
  build-and-run:
    name: Build and run
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build Code
        run: pwd && whoami && ls -l
        
